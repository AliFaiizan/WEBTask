// All of the action types that we have can be added in this file. This way, we can avoid typos and errors in our code. If we misspell an action type, we will get an error. This is a good way to avoid errors in our code.
export enum ActionType {
  SEARCH_MOVIES = "search_movies",
  SEARCH_MOVIES_SUCCESS = "search_movies_success",
  SEARCH_MOVIES_ERROR = "search_movies_error",
}
